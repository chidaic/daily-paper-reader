---
title: "Orchestrated Reality: From Role-Play to Living, Playable Game Worlds -- LLM-Driven World Simulation as a Parameterized-Action POMDP"
title_zh: 编排现实：从角色扮演到生动可玩的游戏世界——将LLM驱动的世界模拟作为参数化动作POMDP
authors: "Yuhang Huang, Chenmiao Li, Chaowei Fang"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.16014v1"
tags: ["query:player-ai"]
score: 9.0
evidence: LLM驱动的游戏世界模拟，包含NPC角色扮演和持久化状态
tldr: 传统游戏叙事与模拟融合成本高昂，LLM驱动的世界面临状态不一致问题。提出编排现实框架，将世界视为单例编排代理拥有的规范JSON对象，形式化为参数化动作POMDP，采用Plan-Diff-Validate-Apply流水线保证状态可验证。在真实部署中收集15个事件示例，验证了框架能协调叙事、规则和状态。为LLM驱动可持续游戏世界提供形式化基础，未来可扩展到多NPC和强化学习环境。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16014v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1338, \"height\": 476, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16014v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1443, \"height\": 1094, \"label\": \"Figure\"}]"
motivation: 传统游戏叙事与模拟结合成本高，LLM世界缺乏持久状态一致性，导致完全自主引擎不可行。
method: 提出编排现实框架，将世界形式化为参数化动作POMDP，用PDVA流水线提交模式验证的JSON增量。
result: 给出形式化模型与15个真实部署事件，展示了框架在叙事、规则和状态协调上的可行性。
conclusion: 为LLM驱动的持久游戏世界提供架构方案，未来可支持多NPC并发和作为RL环境。
---

## 摘要
许多游戏依赖于故事叙述与追踪等级、NPC行为和后果模拟的系统相结合；将紧密编写的叙事与深度模拟的世界相融合——在沙盒和开放世界设定中尤为突出——成本高得令人望而却步。LLM驱动的世界开辟了一条新路径：单一框架可以协调数值状态、叙事语气、故事节奏和规则逻辑。实现这一点需要LLM系统维持一个持久的世界（谁在哪里、刚刚发生了什么、当前为真的事实），而当今部署的系统无法做到：叙事语气以自由散文形式断言状态，没有任何经过验证的表示，因此完全自主的游戏引擎仍然不可行。我们将此视为架构选择，而非语言模型的局限性，并报告一个正在进行中的框架——编排现实——该框架将世界视为一个由类似于桌面RPG游戏主持人的单例编排代理拥有的规范对象。我们将面向人类玩家的LLM驱动游戏世界形式化为参数化动作POMDP：状态是一个规范JSON实体树，动作分解为$a=(k, x_k)$（一个离散意图种类加上结构化JSON参数），代理仅观察状态的叙事投影$o=O(s)$，转移核$F$是一个LLM驱动的计划-差异-验证-应用（PDVA）管道，提交经过模式验证、内容哈希的JSON差异。我们给出了形式化模型、一个JSON状态示例、一个完整的单轮示例，以及来自实际部署的15个说明性事件目录，展示了该框架的运行。通过计划的人类玩家研究进行的实证验证——以及多NPC并发代理和作为RL环境的部署——定位为未来工作。

## Abstract
Many games rely on storytelling combined with systems that track levelling, NPC behaviour, and consequence simulation; bridging tightly-authored narrative with deeply-simulated worlds -- most acute in sandbox and open-world settings -- has been prohibitively expensive. LLM-driven worlds open a new path: a single harness can coordinate numerical state, narrative voice, storytelling pacing, and rule logic together. Realising this requires the LLM system to sustain a persistent world (who is where, what has just happened, what is currently true), which today's deployed systems do not: the narrative voice asserts state in free prose without any validated representation, so a fully autonomous game engine remains infeasible. We treat this as an architectural choice, not a limitation of language models, and report work in progress on a framework -- orchestrated reality -- that makes the world a canonical object owned by a singleton orchestration agent analogous to the tabletop-RPG Game Master (GM). We formalise an LLM-driven game world for a human player as a Parameterized-Action POMDP: state is a tree of canonical JSON entities, actions decompose as $a=(k, x_k)$ (a discrete intent kind plus structured JSON parameters), the agent observes only a narrative projection $o=O(s)$ of state, and the transition kernel $F$ is an LLM-driven Plan-Diff-Validate-Apply (PDVA) pipeline that commits schema-validated, content-hashed JSON deltas. We give the formal model, a JSON-state example, a worked single-turn example, and a catalogue of 15 illustrative incidents drawn from a real deployment showing the framework in action. Empirical validation through a planned human player study -- together with multi-NPC concurrent agency and deployment as an RL environment -- is situated as future work.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：许多游戏依赖故事叙述与系统（等级追踪、NPC行为、后果模拟）相结合，但在沙盒和开放世界设定中，将紧密编写的叙事与深度模拟的世界融合成本极高。LLM驱动的世界开辟了新路径，但当前LLM系统无法维持持久、一致的世界状态（谁在哪里、刚刚发生了什么、当前为真的事实），导致完全自主的游戏引擎不可行。
- **核心问题**：当前LLM叙事系统以自由散文形式断言状态，缺乏经过验证的表示，导致三个反复出现的失败模式：（1）无状态性：会话结束时世界消失，“记忆”仅限于上下文窗口的邻近性；（2）未经验证的写入：模型可断言任何世界变化，状态无声漂移并积累矛盾；（3）单体代理：一个提示同时叙述环境、为每个角色配音并裁决规则，混淆了本应分离的角色。
- **整体含义**：作者将问题视为架构选择而非语言模型限制，提出“编排现实”框架，使世界成为规范对象，由类似桌面RPG游戏主持人（GM）的单例编排代理拥有，通过验证管道提交状态变更，实现持久、可审计的游戏世界。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 将面向人类玩家的LLM驱动游戏世界形式化为 **参数化动作POMDP**（Partially Observable Markov Decision Process with Parameterized Actions）。
- 世界状态 $s \in \mathcal{S}$ 是磁盘上一棵规范JSON实体树，每个实体有类型化模式（schema），可通过内容哈希（SHA256）寻址和区分。
- 动作定义为 $a = (k, x_k)$，其中 $k$ 是有限意图种类（如move, speak, give, inspect），$x_k$ 是 $k$ 特定的结构化JSON参数。
- 代理不直接观察状态 $s_t$，而是通过叙事投影 $o_t \sim O(s_t)$ 接收自然语言叙述和表面信息（位置、时间、物品栏）。

### 关键技术细节：Plan–Diff–Validate–Apply (PDVA) 流水线
- **Plan**：根据状态 $s_t$ 和动作 $a_t$，构建有界上下文 $p = \text{Context}(s_t, a_t)$（相关状态切片）。
- **Diff**：LLM模型返回 $(narrative, \Delta_t)$：叙述 $o_{t+1}$ 和结构化提议变更 $\Delta_t = \{(\text{path}_j, \text{op}_j, \text{value}_j)\}$。
- **Validate**：每个变更 $\delta_j$ 须同时通过三项检查：
  - **SchemaOK**：符合JSON模式；
  - **PermOK**：属于提议者的许可范围 $c$；
  - **RuleOK**：满足游戏规则 $\mathcal{R}$。
- **Apply**：所有有效变更原子提交，新状态 $s_{t+1} = \text{Apply}(s_t, \{\delta_j : \text{Valid}(\delta_j)\})$，并计算内容哈希 $h(s_{t+1})$。

### 马尔可夫性质构造
- 通过事件溯源+内容哈希，所有相关历史折叠进 $s_t$，由 $h(s_t)$ 标识，而非无限增长的提示。
- 验证作为转换守卫，防止散文无声改变状态。
- 转移核 $F$ 是严格马尔可夫的——LLM仅被降级为在 $\Delta_t$ 内部提出提案。

### 世界代理
- 单例世界代理拥有世界目录、回合循环和提交事务。
- 每回合组装上下文包（固定种子集+相关性排序记录），直接注入规范JSON条目到模型提示中。
- 代理合约 $c = (\text{id}, \text{type}, R, W)$ 定义读写范围，默认拒绝优先，确保“世界拥有世界”是检查的不变量。

## 3. 实验设计：数据集、场景、基准与对比方法

- **数据集/场景**：没有使用公开数据集。论文基于真实部署的引擎，由少量测试者（N≈5）在三个月内非正式使用，从引擎播放历史（会话追踪、叙述游戏过程、事件日志）中挖掘出15个事件实例（incidents），分为三类：P1（多代理编排，9个）、P2（JSON强制记忆注入，5个）、P3（JSON状态流，1个）。
- **基准（Benchmark）**：未提供正式基准。论文计划进行受控玩家研究，但尚未执行。目前有一个预实验装置：包括无引擎单LLM基线、相同LLM每回合注入规范JSON、完整多代理循环，由LLM裁判评分，但规模太小（n=1-3场景，少数回合，单次运行），裁判方差占主导，结论不可靠。
- **对比方法**：论文在相关工作中讨论了Generative Agents、Voyager、MineDojo、RoleLLM、Character-LLM、Dramatron、Agents' Room、AutoGen、MetaGPT、CAMEL等，但未进行直接定量对比。实验设计是规划中的受试者内实验（2条件×3 session），条件A为无状态聊天角色扮演（单LLM+角色提示，无持久世界状态），条件B为持久循环（世界代理/角色代理架构），使用同一LLM后端和同一场景。

## 4. 资源与算力

- **未明确说明**：论文未提及使用的GPU型号、数量、训练时长或推理预算。仅在开放问题中提到“多NPC并发后每个角色一个模型实例会使API花费倍增”，暗示使用了基于API的LLM服务。整体框架属于推理阶段应用，未涉及模型训练。

## 5. 实验数量与充分性

- **实验数量**：非常有限。仅有一次粗略的非正式测试（N≈5测试者，三个月），从实际播放历史中提取15个事件示例。没有进行正式的用户研究或量化对比实验。计划中的受试者内研究（2条件×3 session，各30分钟）尚未执行。
- **充分性与公平性**：
  - **不充分**：当前缺乏严格的假设检验、效应量估计、统计功效分析。作者自己也承认“所有玩家体验的主张仍是开放的”。
  - **偏差风险**：非正式测试可能存在选择偏差（只有少量早期测试者），15个事件示例是精心挑选的有利案例。没有与控制条件（无框架）进行系统性对比。
  - **公平性**：计划的研究设计采用了受试者内设计、counterbalancing、标准化问卷（miniPXI和PAX），如果执行将具有较好的公平性，但目前未执行。

## 6. 论文的主要结论与发现

- **理论贡献**：成功将LLM驱动的游戏世界形式化为参数化动作POMDP，使世界状态成为规范的、可审计的对象，转移核通过PDVA流水线执行验证提交。
- **架构可行性**：通过15个真实部署事件示例，展示了三种机制（多代理编排、JSON强制记忆注入、JSON状态流）的实际运行，表明框架在实践中可工作。
- **早期假设**：基于非正式测试提出三个待检验假设：
  - H1：前提条件触发的环境事件即使没有角色说话，也感觉“世界是活的”；
  - H2：经叙述的验证拒绝（如规则检查失败）会增加后果感而非破坏沉浸感；
  - H3：可检查、哈希标识的状态降低了世界创作和调试的门槛。
- **开放问题**：确定LLM随机性下的可复现性、每行动成本纪律、未指定细节下的叙事漂移（如NPC伤疤左右手不一致）、规则覆盖率不完全等。

## 7. 优点：方法或实验设计上的亮点

- **形式化清晰**：将游戏世界模拟明确定义为参数化动作POMDP，为后续理论分析和多代理扩展提供了数学基础。
- **架构创新**：通过PDVA流水线和JSON规范状态，解决了LLM世界状态不一致的核心问题，使世界可检查、可分支、可通过内容哈希回放。
- **事件溯源与审计日志**：每个状态变更都记录为事件，包含哈希链，支持调试和分析。
- **模块化设计**：世界代理、专家子代理（规则、战斗、环境事件）、未来NPC心智分离，各司其职。
- **关注可复制性**：明确强调状态是由内容哈希标识的，而不是提示中的文本。
- **计划中的研究设计合理**：使用了经过验证的标准化问卷（miniPXI、PAX）、受试者内设计、counterbalancing、混合方法（定量+定性访谈），若执行将提供有力证据。

## 8. 不足与局限

- **实验覆盖严重不足**：目前只有非正式的小规模测试和选择性事件示例，缺乏任何统计意义的结果。所有玩家体验主张完全未经验证。
- **未提供对比实验**：没有与现有方法（如Generative Agents、Dramatron等）进行公平定量比较。
- **资源与成本未分析**：未报告推理成本、延迟、可扩展性等实际部署指标。多NPC并发时的成本增长未量化。
- **叙事漂移问题未解决**：只验证JSON模式中的字段，游离状态（如NPC细节的细微变化）可能漂移，该问题仅被提及而未解决。
- **安全性/偏见风险**：未讨论LLM生成内容的安全问题、偏见、违规内容过滤等。
- **计划研究局限**：受试者内设计可能存在顺序效应（尽管计划了counterbalancing），且仅使用同一LLM后端（泛化性未知）。
- **作者明确声明**：论文是“work in progress”，所有实证结果均为未来工作，当前所有主张需谨慎对待。

（完）
